object question01 {


    def main(args: Array[String]) = {
        var (k,i,j) = (2,2,2)
        var (m,n) = (5,5)
        var (f,g) = (12.0,4.0)
        var c = 'X'
        println("a) k + 12 * m = " + k+12*m)
        println("b) m / j = " + m/j)
        println("c) n % j = " + n%j)
        println("d) m / j * j = " + m/j*j)
        println("e) f + 10 * 5 + g = " + f+10*5+g)
        println("f) ++i * n = " + (i+1)*n)
    }
}